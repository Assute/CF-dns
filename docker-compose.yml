version: '3.8'

services:
  cf-cdn:
    build: .
    container_name: cf-cdn-manager
    ports:
      - "3600:3600"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    environment:
      - NODE_ENV=production
